# Basic Demo

This directory contains a basic demonstration of reshuffle's functionality. It showcases how reshuffle can be used to
distribute data across multiple processors efficiently. The demo includes a simple example that demonstrates how a
matrix can be distributed across multiple processors, and how this distribution can easily be changed using our library.

To run, execute the following command:

```shell
mpirun -np 4 ./basic_demo.out
```
