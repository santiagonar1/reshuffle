add_subdirectory(basic_demo)
add_subdirectory(heat_transfer)
add_subdirectory(elastic_demo)

if (RESHUFFLE_BUILD_DYN_PROC_DEMO)
    message(STATUS "Dyn proc demo will be compiled")
    add_subdirectory(dyn_proc_demo)
else ()
    message(STATUS "Dyn proc demo will NOT be compiled")
endif ()

if (RESHUFFLE_PROFILING)
    message(STATUS "Profiling demo will be compiled")
    add_subdirectory(profiling)
else ()
    message(STATUS "Profiling demo will NOT be compiled")
endif ()

if (RESHUFFLE_TEST_SCALAPACK)
    message(STATUS "ScaLAPACK demo will be compiled")
    add_subdirectory(scalapack_demo)
else ()
    message(STATUS "ScaLAPACK demo will NOT be compiled")
endif ()